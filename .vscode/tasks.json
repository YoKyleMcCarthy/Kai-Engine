{
    "tasks": [
        {
            "type": "shell",
            "label": "build everything",
            "osx": { "command": "${workspaceFolder}/build-mac.sh" },
            "windows": { "command": "./build-win.bat" },
            "problemMatcher": ["$gcc"],
            "options": { "cwd": "${workspaceFolder}" },
            "group": { "kind": "build", "isDefault": true }
        },
    ],
    "version": "2.0.0"
}